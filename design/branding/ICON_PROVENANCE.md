# TrackMeUp icon provenance

This record accompanies `trackmeup-icon-reference.png` and the application
identity assets generated from it under `TrackMeUp/Assets/`.

Repository history first records the reference image in commit `0f7ce7d` on
2026-08-05 (`feat: establish initial TrackMeUp application baseline`). The
repository script `scripts/TrackMeUp.ps1` uses that reference to generate the
TrackMeUp app icon, Store logo, square and wide logos, and splash-screen
variants. No upstream source, generation method or prompt, or applicable
license/service terms are recorded alongside the reference image.

For repository license-scope purposes, the reference and its identity
derivatives are treated as TrackMeUp Brand Assets governed by the
[Trademark and Brand Policy](../../TRADEMARKS.md), not by the MIT License. That
classification and the commit record do not establish authorship or
redistribution rights.

Before public source or binary redistribution, the project owner must document
the reference image's source and method, confirm any applicable service terms,
and confirm the intended publication scope. If those checks cannot be
completed, replace the reference and regenerate every derivative. Forks and
independent distributions must replace the TrackMeUp identity assets unless
separate permission has been granted. See the repository-wide
[asset licensing record](../../ASSET_LICENSING.md). This record is not legal
advice.
