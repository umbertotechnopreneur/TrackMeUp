<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TrackMeUp.Controls.OptionsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WidthStates">
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="ApiKeyStates">
                <VisualState x:Name="ApiKeyNeedsAttention">
                    <VisualState.Setters>
                        <Setter Target="ApiKeyStatusIcon.Foreground" Value="{ThemeResource SystemFillColorCautionBrush}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ApiKeyReady">
                    <VisualState.Setters>
                        <Setter Target="ApiKeyStatusIcon.Foreground" Value="{ThemeResource SystemFillColorSuccessBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppOptionsView">
            <ScrollViewer
                HorizontalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Auto">
                <StackPanel Padding="0,0,18,12" Spacing="0">
                    <TextBlock Tag="Options.Theme.Header" Style="{StaticResource BodyStrongTextBlockStyle}" Text="Theme" />
                    <Grid Margin="0,8,0,0" ColumnSpacing="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ToggleButton
                            x:Name="ThemeSystemButton"
                            MinHeight="36"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            AutomationProperties.Name="System theme"
                            Click="ThemeButton_Click"
                            IsChecked="True">
                            <TextBlock Tag="Options.Theme.system" Text="System" />
                        </ToggleButton>
                        <ToggleButton
                            x:Name="ThemeLightButton"
                            Grid.Column="1"
                            MinHeight="36"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            AutomationProperties.Name="Light theme"
                            Click="ThemeButton_Click">
                            <TextBlock Tag="Options.Theme.light" Text="Light" />
                        </ToggleButton>
                        <ToggleButton
                            x:Name="ThemeDarkButton"
                            Grid.Column="2"
                            MinHeight="36"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            AutomationProperties.Name="Dark theme"
                            Click="ThemeButton_Click">
                            <TextBlock Tag="Options.Theme.dark" Text="Dark" />
                        </ToggleButton>
                    </Grid>

                    <ComboBox
                        x:Name="PositionBox"
                        Margin="0,16,0,0"
                        Tag="Options.Position"
                        HorizontalAlignment="Stretch"
                        Header="Player position">
                        <ComboBoxItem Content="Bottom center" Tag="bottom-center" />
                        <ComboBoxItem Content="Bottom left" Tag="bottom-left" />
                        <ComboBoxItem Content="Bottom right" Tag="bottom-right" />
                        <ComboBoxItem Content="Top left" Tag="top-left" />
                        <ComboBoxItem Content="Top right" Tag="top-right" />
                    </ComboBox>

                    <Border
                        Height="1"
                        Margin="0,18,0,0"
                        Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
                    <StackPanel Margin="0,14,0,0" Spacing="2">
                        <TextBlock
                            Tag="Options.Section.TaskbarWidget"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="Windows taskbar widget" />
                        <TextBlock
                            Tag="Options.Section.TaskbarWidget.Description"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="Show quick controls and recording status in the Windows taskbar."
                            TextWrapping="Wrap" />
                    </StackPanel>
                    <Grid MinHeight="52" Margin="0,6,0,0" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Tag="Options.TaskbarWidget.Visible"
                            VerticalAlignment="Center"
                            Text="Visible" />
                        <ToggleSwitch
                            x:Name="TaskbarWidgetVisibleSwitch"
                            Grid.Column="1"
                            MinWidth="0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="Visible"
                            OffContent=""
                            OnContent=""
                            Toggled="TaskbarWidgetVisibleSwitch_Toggled" />
                    </Grid>
                    <ComboBox
                        x:Name="TaskbarWidgetPositionBox"
                        Margin="0,8,0,0"
                        Tag="Options.TaskbarPosition"
                        HorizontalAlignment="Stretch"
                        Header="Position"
                        IsEnabled="False">
                        <ComboBoxItem Content="Left" Tag="left" />
                        <ComboBoxItem Content="Right" Tag="right" />
                    </ComboBox>

                    <StackPanel Margin="0,14,0,0" Spacing="2">
                        <TextBox
                            x:Name="ScreenshotFolderBox"
                            Tag="Options.SnapshotFolder"
                            Header="Screen-capture folder"
                            PlaceholderText="Folder for screen captures" />
                        <HyperlinkButton
                            x:Name="OpenScreenshotFolderButton"
                            Padding="0,5,0,0"
                            HorizontalAlignment="Left"
                            AutomationProperties.Name="Open snapshot and debug-image folder"
                            Click="OpenScreenshotFolderButton_Click"
                            ToolTipService.ToolTip="Open snapshot and debug-image folder">
                            <StackPanel Orientation="Horizontal" Spacing="7">
                                <FontIcon
                                    AutomationProperties.AccessibilityView="Raw"
                                    FontFamily="Segoe Fluent Icons"
                                    FontSize="14"
                                    Glyph="&#xE8A7;" />
                                <TextBlock
                                    Tag="Options.OpenFolderAction"
                                    Text="Open snapshot and debug-image folder" />
                            </StackPanel>
                        </HyperlinkButton>
                    </StackPanel>

                    <Grid MinHeight="52" Margin="0,10,0,0" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Spacing="1">
                            <TextBlock Tag="Options.KeepSnapshots.Header" Text="Keep screen captures" TextWrapping="Wrap" />
                            <TextBlock
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Tag="Options.KeepSnapshots.On"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Keep on this PC"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <ToggleSwitch
                            x:Name="KeepScreenshotsSwitch"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"
                            MinWidth="0"
                            AutomationProperties.Name="Keep screen captures"
                            OffContent=""
                            OnContent="" />
                    </Grid>

                    <Button
                        Margin="0,12,0,0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        AutomationProperties.Name="Open AI configuration"
                        Click="AiSettingsButton_Click"
                        Padding="12,10">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <FontIcon
                                VerticalAlignment="Center"
                                AutomationProperties.AccessibilityView="Raw"
                                FontFamily="Segoe Fluent Icons"
                                FontSize="17"
                                Glyph="&#xE945;" />
                            <StackPanel Grid.Column="1" Spacing="1">
                                <TextBlock Tag="Options.AiConfiguration.Title" Style="{StaticResource BodyStrongTextBlockStyle}" Text="AI configuration" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Tag="Options.AiConfiguration.Subtitle"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="Model, key and thinking effort"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                            <FontIcon
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                AutomationProperties.AccessibilityView="Raw"
                                FontFamily="Segoe Fluent Icons"
                                FontSize="12"
                                Glyph="&#xE76C;" />
                        </Grid>
                    </Button>

                    <Button
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        AutomationProperties.Name="Advanced app options"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Click="GeneralAdvancedButton_Click"
                        Padding="4,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Tag="Options.Advanced" Text="Advanced options" />
                            <FontIcon
                                x:Name="GeneralAdvancedChevron"
                                Grid.Column="1"
                                AutomationProperties.AccessibilityView="Raw"
                                FontFamily="Segoe Fluent Icons"
                                FontSize="12"
                                Glyph="&#xE70D;" />
                        </Grid>
                    </Button>

                    <StackPanel x:Name="GeneralAdvancedPanel" Margin="0,8,0,0" Spacing="10" Visibility="Collapsed">
                        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <ComboBox x:Name="LanguageBox" Tag="Options.Language" HorizontalAlignment="Stretch" Header="Interface language">
                            <ComboBoxItem Content="System" Tag="system" />
                            <ComboBoxItem Content="English" Tag="en" />
                            <ComboBoxItem Content="Italiano" Tag="it" />
                            <ComboBoxItem Content="Français" Tag="fr" />
                            <ComboBoxItem Content="Deutsch" Tag="de" />
                            <ComboBoxItem Content="Español" Tag="es" />
                            <ComboBoxItem Content="Tiếng Việt" Tag="vi" />
                        </ComboBox>

                        <Border Height="1" Margin="0,4,0,0" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <StackPanel Spacing="2">
                            <TextBlock Tag="Options.Section.Startup" Style="{StaticResource BodyStrongTextBlockStyle}" Text="Startup" />
                            <TextBlock
                                Tag="Options.Section.Startup.Description"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Choose how TrackMeUp behaves after Windows sign-in."
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <Grid MinHeight="44" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" Tag="Options.StartWithWindows.Header" Text="Start with Windows" TextWrapping="Wrap" />
                            <ToggleSwitch
                                x:Name="StartWithWindowsSwitch"
                                Grid.Column="1"
                                MinWidth="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="Start with Windows"
                                OffContent=""
                                OnContent="" />
                        </Grid>
                        <Grid MinHeight="44" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" Tag="Options.StartTracking.Header" Text="Start tracking on launch" TextWrapping="Wrap" />
                            <ToggleSwitch
                                x:Name="StartTrackingOnLaunchSwitch"
                                Grid.Column="1"
                                MinWidth="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="Start tracking on launch"
                                OffContent=""
                                OnContent="" />
                        </Grid>

                        <Border Height="1" Margin="0,4,0,0" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <StackPanel Spacing="2">
                            <TextBlock Tag="Options.Section.Snapshots" Style="{StaticResource BodyStrongTextBlockStyle}" Text="Screen captures" />
                            <TextBlock
                                Tag="Options.Section.Snapshots.Description"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Choose when screen captures are made and how long they stay here."
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <Grid MinHeight="44" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" Tag="Options.SnapshotsEnabled.Header" Text="Allow screen captures" TextWrapping="Wrap" />
                            <ToggleSwitch
                                x:Name="ScreenshotsEnabledSwitch"
                                Grid.Column="1"
                                MinWidth="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="Allow screen captures"
                                OffContent=""
                                OnContent="" />
                        </Grid>
                        <ComboBox x:Name="ScreenshotModeBox" Tag="Options.SnapshotMode" HorizontalAlignment="Stretch" Header="What to capture" SelectionChanged="ScreenshotModeBox_SelectionChanged">
                            <ComboBoxItem Content="All displays" Tag="all-screens" />
                            <ComboBoxItem Content="Active window" Tag="active-window" />
                        </ComboBox>
                        <TextBox x:Name="ScreenshotModeHintBox" Tag="Options.SnapshotResult" Header="Result" IsReadOnly="True" IsTabStop="False" TextWrapping="Wrap" />
                        <Grid MinHeight="44" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" Tag="Options.Watermark.Header" Text="Screen-capture label" TextWrapping="Wrap" />
                            <ToggleSwitch
                                x:Name="WatermarkSwitch"
                                Grid.Column="1"
                                MinWidth="0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="Screen-capture label"
                                OffContent=""
                                OnContent="" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Grid x:Name="AiOptionsView" Visibility="Collapsed">
            <ScrollViewer
                HorizontalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Auto">
                <StackPanel Padding="0,0,18,12" Spacing="0">
                    <Grid MinHeight="44" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            Tag="Options.OpenAi.Header"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="OpenAI integration"
                            TextWrapping="Wrap" />
                        <ToggleSwitch
                            x:Name="OpenAiEnabledSwitch"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="OpenAI integration"
                            IsEnabled="{Binding CanToggle, Mode=OneWay}"
                            IsOn="{Binding Enabled, Mode=OneWay}"
                            OffContent=""
                            OnContent=""
                            Toggled="OpenAiEnabledSwitch_Toggled" />
                    </Grid>

                    <StackPanel
                        Margin="0,8,0,0"
                        Orientation="Horizontal"
                        Spacing="8">
                        <FontIcon
                            x:Name="ApiKeyStatusIcon"
                            VerticalAlignment="Center"
                            AutomationProperties.AccessibilityView="Raw"
                            FontFamily="Segoe Fluent Icons"
                            FontSize="16"
                            Foreground="{ThemeResource SystemFillColorCautionBrush}"
                            Glyph="&#xE7BA;" />
                        <TextBlock
                            x:Name="ApiKeyStatusText"
                            VerticalAlignment="Center"
                            AutomationProperties.LiveSetting="Polite"
                            Style="{StaticResource BodyStrongTextBlockStyle}"
                            Text="API key status unavailable."
                            TextWrapping="Wrap" />
                    </StackPanel>

                    <Expander
                        x:Name="ApiKeyExpander"
                        Margin="0,6,0,0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        AutomationProperties.Name="Set key"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        SizeChanged="ApiKeyExpander_SizeChanged">
                        <Expander.Header>
                            <TextBlock
                                x:Name="ApiKeyActionText"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="Set key" />
                        </Expander.Header>
                        <StackPanel Padding="0,2,0,6" Spacing="0">
                            <PasswordBox
                                x:Name="ApiKeyBox"
                                Tag="Options.ApiKey"
                                Header="API key"
                                PasswordRevealMode="Peek"
                                PlaceholderText="Used from Windows; not copied by TrackMeUp" />
                            <TextBlock
                                Margin="0,5,0,0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Tag="Options.AiKeyStorageHint"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Stored only in your Windows user environment."
                                TextWrapping="Wrap" />
                            <Button
                                Margin="0,8,0,0"
                                Tag="Options.SaveApiKey"
                                HorizontalAlignment="Stretch"
                                Click="SetApiKeyButton_Click"
                                Content="Set key for this PC" />
                        </StackPanel>
                    </Expander>

                    <ComboBox
                        x:Name="ModelBox"
                        Margin="0,12,0,0"
                        Tag="Options.Model"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Header="Model"
                        SelectionChanged="ModelBox_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid MinWidth="294" ColumnSpacing="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border
                                        Width="10"
                                        Height="10"
                                        VerticalAlignment="Center"
                                        Background="{Binding AccentBrush}"
                                        CornerRadius="5" />
                                    <StackPanel Grid.Column="1" Spacing="1">
                                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding Name}" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{Binding Key}" />
                                    </StackPanel>
                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{Binding CapabilityLabel}" />
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Border
                        x:Name="ModelInfoCard"
                        Margin="0,7,0,0"
                        Padding="10"
                        Background="{ThemeResource ControlFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Visibility="Collapsed">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="ModelAccentBar" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="2" />
                            <StackPanel Grid.Column="1" Spacing="3">
                                <TextBlock
                                    x:Name="ModelDescriptionText"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    TextWrapping="Wrap" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock x:Name="ModelKeyText" Style="{StaticResource CaptionTextBlockStyle}" />
                                    <Border
                                        x:Name="ModelPreviewBadge"
                                        Padding="6,1"
                                        Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                        CornerRadius="8"
                                        Visibility="Collapsed">
                                        <TextBlock FontSize="11" Tag="Options.ModelPreview" Text="Preview" />
                                    </Border>
                                    <Border
                                        x:Name="ModelCapabilityBadge"
                                        Padding="6,1"
                                        Background="{ThemeResource ControlFillColorTertiaryBrush}"
                                        CornerRadius="8"
                                        Visibility="Collapsed">
                                        <TextBlock x:Name="ModelCapabilityText" FontSize="11" />
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ComboBox
                        x:Name="AiReasoningEffortBox"
                        Margin="0,10,0,0"
                        Tag="Options.Reasoning"
                        HorizontalAlignment="Stretch"
                        Header="Thinking effort">
                        <ComboBoxItem Content="Automatic" Tag="auto" />
                        <ComboBoxItem Content="None" Tag="none" />
                        <ComboBoxItem Content="Low" Tag="low" />
                        <ComboBoxItem Content="Medium" Tag="medium" />
                        <ComboBoxItem Content="High" Tag="high" />
                        <ComboBoxItem Content="Very high" Tag="xhigh" />
                        <ComboBoxItem Content="Maximum" Tag="max" />
                    </ComboBox>

                    <Button
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        AutomationProperties.Name="Advanced AI options"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Click="AiAdvancedButton_Click"
                        Padding="4,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Tag="Options.Advanced" Text="Advanced options" />
                            <FontIcon
                                x:Name="AiAdvancedChevron"
                                Grid.Column="1"
                                AutomationProperties.AccessibilityView="Raw"
                                FontFamily="Segoe Fluent Icons"
                                FontSize="12"
                                Glyph="&#xE70D;" />
                        </Grid>
                    </Button>

                    <StackPanel x:Name="AiAdvancedPanel" Margin="0,8,0,0" Spacing="10" Visibility="Collapsed">
                        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
                        <ComboBox x:Name="AiOutputDetailBox" Tag="Options.OutputDetail" HorizontalAlignment="Stretch" Header="Analysis detail">
                            <ComboBoxItem Content="Compact · lowest usage" Tag="compact" />
                            <ComboBoxItem Content="Balanced" Tag="balanced" />
                            <ComboBoxItem Content="Detailed · larger budget" Tag="detailed" />
                        </ComboBox>
                        <TextBox x:Name="AiCustomPromptBox" Tag="Options.CustomPrompt" AcceptsReturn="True" Header="Additional AI instruction (optional)" MinHeight="76" PlaceholderText="Example: Prioritize concise, factual descriptions." TextWrapping="Wrap" />
                        <ToggleSwitch x:Name="IncludeDeviceLocationSwitch" Tag="Options.DeviceLocation" Header="Include Windows location in AI requests" OffContent="Off" OnContent="Windows Location" />
                        <ComboBox x:Name="AiProviderBox" Tag="Options.AiProvider" HorizontalAlignment="Stretch" Header="AI service" SelectionChanged="AiProviderBox_SelectionChanged">
                            <ComboBoxItem Content="OpenAI" Tag="openai" />
                            <ComboBoxItem Content="OpenRouter" Tag="openrouter" />
                            <ComboBoxItem Content="Anthropic" Tag="anthropic" />
                        </ComboBox>
                        <TextBox x:Name="AiEndpointBox" Tag="Options.AiEndpoint" Header="Service address" />
                        <TextBox x:Name="AiApiKeyNameBox" Tag="Options.AiKeyVariable" Header="Windows key name" />

                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Border
            Grid.Row="1"
            Padding="0,10,18,0"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <StackPanel Spacing="6">
                <Button
                    x:Name="SaveOptionsButton"
                    Tag="Options.Save"
                    HorizontalAlignment="Stretch"
                    Click="SaveButton_Click"
                    Content="Save options"
                    Style="{StaticResource AccentButtonStyle}" />
                <TextBlock
                    x:Name="StatusText"
                    AutomationProperties.LiveSetting="Polite"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
