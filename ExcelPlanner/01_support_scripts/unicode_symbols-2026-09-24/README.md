# Unicode symbols

This folder contains the helper scripts and verification notes for the Unicode weather and holiday symbols used by the workbook.

Run the symbol scripts only when changing the workbook assets or checking layout. Keep the source symbols and their verification output aligned with the workbook version. The symbols are presentation data; they do not change availability, holiday, or time-zone calculations.
